{
  "name": "rpg-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Party-based dungeon crawler prototype. Static, no build step, zero dependencies.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "sim": "node tests/sim.js",
    "serve": "python3 -m http.server 8080"
  }
}
